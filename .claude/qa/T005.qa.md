## Manual QA — T005

**Prerequisites:** Node.js + npx available (no install needed — `@redocly/cli` runs via `npx --yes`). Run all commands from the project root, `C:\Users\anmol.jain\Downloads\ShareConnectSave`.

1. Run `npx --yes @redocly/cli lint contracts/openapi/*.yaml`
   Expect: exits with only `security-defined`, `no-empty-servers`, `info-license`, and `tag-description` findings — zero findings about unresolved `$ref`s, invalid schema types, or malformed paths. (The `security-defined` errors are expected and correct for this project — do not "fix" them by adding a `securitySchemes` block.)

2. Run `npx --yes @redocly/cli bundle contracts/openapi/user-service.yaml -o C:\Users\ANMOL~1.JAI\AppData\Local\Temp\claude\C--Users-anmol-jain-Downloads-ShareConnectSave\ac24d9d7-501b-4f80-b8f0-eecc785672a6\scratchpad\user-service.bundled.yaml`
   Expect: command succeeds (exit code 0) and produces a single bundled file with all `error-envelope.yaml` `$ref`s inlined — proves the cross-file reference resolves cleanly. Repeat for each of the other 7 service files, swapping the input/output filename, if you want full confidence rather than spot-checking one file.

3. Open `REQUIREMENTS.md` side by side with `contracts/openapi/user-service.yaml` in your IDE. Walk `REQUIREMENTS.md` §1 and §2 line by line; for each bullet, confirm a matching `operationId` exists in the YAML.
   Expect: every bullet in §1 (Google auth, OTP, identity verification, base-photo prerequisite) and §2 (profile fields, status toggle) maps to an operation — `googleAuth`, `sendOtp`, `verifyOtp`, `submitIdentityVerification`, `getIdentityStatus`, `getProfile`, `updateProfile`, `uploadProfilePhoto`.

4. Repeat step 3 for each remaining service file against its corresponding REQUIREMENTS.md section (§3–§4 → discovery-service.yaml, §5 → connection-service.yaml, §6 → chat-service.yaml, §7 → rating-service.yaml, §8 → report-service.yaml, §9 → user-service.yaml blocks, §10 → discovery-service.yaml women-only, §11 → admin-service.yaml).
   Expect: no functional requirement bullet is left without a corresponding operation; the only intentional absence is a Chat message-history endpoint (§6, "no history endpoint exists by design").

5. Run `Select-String -Path contracts\openapi\*.yaml -Pattern "securitySchemes|^security:"` (PowerShell) or `grep -rn "securitySchemes\|^security:" contracts/openapi/*.yaml` (bash).
   Expect: zero matches across all 9 files (8 service specs + error-envelope.yaml) — confirms JWT is never modeled as something these services validate themselves.

**Cleanup:** Delete any bundled output files written to the scratchpad in step 2 — they're a validation byproduct, not a deliverable, and should not be committed.
