---
id: T012
title: JWT Validation Middleware
phase: 2
phase-name: API Gateway (.NET)
service: api-gateway
language: .NET
skills: [dotnet-minimal-api]
depends: [T011]
status: draft
---

# T012 — JWT Validation Middleware

## Why this ticket exists

Without centralized JWT validation, every backend service would need to independently verify Google tokens, validate signatures, and extract claims — a maintenance nightmare and a security risk if validation logic drifts. This ticket moves that responsibility to the API Gateway, the single entry point. Once validated here, downstream services trust the injected headers and focus only on business logic. This is the security boundary that protects the entire platform.

## What to build

A JWT Bearer authentication middleware for the YARP gateway that:
- Validates incoming JWT signatures against Google's JWKS endpoint
- Checks token expiry and audience (client ID from `process.env.GOOGLE_CLIENT_ID`)
- Extracts `sub` (user ID), `role`, and `gender` claims from valid tokens
- Injects three headers into the downstream request: `X-User-Id`, `X-User-Role`, `X-User-Gender`
- Strips the raw `Authorization` header before forwarding (services never see the JWT itself)
- Bypasses JWT validation entirely for public auth routes: `POST /auth/google`, `POST /auth/otp/send`, `POST /auth/otp/verify`

**Key files to create:**
- `src/api-gateway/Middleware/JwtValidationMiddleware.cs` — middleware that validates tokens and injects headers
- `src/api-gateway/Services/JwksService.cs` — caches Google's JWKS endpoint (public keys for validation)
- `src/api-gateway/Configuration/JwtOptions.cs` — configuration object for Google client ID and other JWT settings
- Update `src/api-gateway/Program.cs` to register the middleware into the request pipeline

## Technical specification

Add JWT Bearer authentication middleware. Validate:
- Signature (Google's JWKS endpoint for the Google Sign-In tokens)
- Expiry
- Audience (your app's client ID from `process.env.GOOGLE_CLIENT_ID`)

On valid JWT: extract `sub` (user ID), `role`, `gender` claims. Inject as headers before forwarding:
```
X-User-Id: <sub>
X-User-Role: <role>
X-User-Gender: <gender>
```

Strip the `Authorization` header before forwarding downstream (services must not receive the raw JWT).

Public routes (no JWT required): `POST /auth/google`, `POST /auth/otp/send`, `POST /auth/otp/verify`.

## Acceptance criteria

- [ ] Request with valid JWT → headers injected, Authorization stripped, proxied
- [ ] Request with expired/invalid JWT → 401 returned, not forwarded
- [ ] Public routes pass through without JWT

## Patterns demonstrated

- **API Gateway Pattern** — Centralize cross-cutting concerns (authentication) at a single entry point. Services trust injected headers and never re-validate the token; this prevents duplication, reduces attack surface, and ensures consistency.
- **Dependency Inversion (D)** — The middleware depends on `IConfiguration` and an abstraction for JWKS retrieval, not hardcoded Google URLs or client IDs. Easy to swap implementations for testing.
- **Middleware Chain** — ASP.NET Core's middleware pipeline is the backbone. JWT validation is one plug-in layer; rate limiting (T013) will be another. Non-blocking composition.
- **Caching for Resilience** — JWKS endpoint is called infrequently and cached. Reduces latency, reduces dependency on Google's availability, matches industry practice (Google's keys rotate on a schedule, not per-request).

## Agent implementation notes

**Key implementation decisions (already made by spec):**
- Google is the token issuer; use Google's JWKS endpoint as the source of truth for public keys
- The audience claim must match `process.env.GOOGLE_CLIENT_ID` — strict validation, no wildcards
- Strip `Authorization` header after validation; downstream services must use only the injected `X-User-*` headers
- Public routes bypass the middleware entirely; use a route-based bypass (e.g., endpoint metadata) not per-request checks

**What to do:**
- Use `Microsoft.IdentityModel.Protocols.OpenIdConnect` and related NuGet packages to handle JWKS and JWT validation
- Implement `JwksService` to cache Google's JWKS for ~24 hours (standard Google key rotation window)
- In the middleware, catch `SecurityTokenException` and return 401 Unauthorized with a clear error message
- Ensure the three headers (`X-User-Id`, `X-User-Role`, `X-User-Gender`) are always present on forwarded requests (or return 401 if a required claim is missing)
- Store the current principal/claims in `HttpContext.User` after validation (ASP.NET Core convention)

**What NOT to do:**
- Do not re-validate the JWT in downstream services; they must trust the injected headers
- Do not log or expose the raw JWT in error responses (only log the validation failure reason)
- Do not hardcode Google's JWKS endpoint URL; fetch it from `https://www.googleapis.com/.well-known/openid-configuration`
- Do not forget that `role` and `gender` may not be present in all tokens — handle missing claims gracefully (default to a safe value or 401)

**Reference skill file:**
- Review `dotnet-minimal-api` skill for patterns on registering middleware and reading configuration in Program.cs
